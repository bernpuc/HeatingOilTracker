<Window x:Class="HeatingOilTracker.Views.DeliveryEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Height="400" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Date -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Date" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <DatePicker SelectedDate="{Binding Date, Mode=TwoWay}"
                        FontSize="14"/>
        </StackPanel>

        <!-- Gallons -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Gallons" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Gallons, UpdateSourceTrigger=PropertyChanged, StringFormat=F1}"/>
        </StackPanel>

        <!-- Price Per Gallon -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Price Per Gallon ($)" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding PricePerGallon, UpdateSourceTrigger=PropertyChanged, StringFormat=F3}"/>
        </StackPanel>

        <!-- Total Cost (computed, read-only) -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Total Cost" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding TotalCost, StringFormat=\{0:C2\}}"
                       FontSize="18" FontWeight="Bold" Foreground="#16a34a"/>
        </StackPanel>

        <!-- Notes -->
        <StackPanel Grid.Row="4" Margin="0,0,0,15">
            <TextBlock Text="Notes" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Height="60"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Command="{Binding CancelCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Margin="0,0,10,0"/>
            <Button Content="Save"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding SaveCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </Grid>
</Window>
